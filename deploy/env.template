# Flask 应用环境变量配置
# 复制此文件为 .env 并修改相应值
# 位置: /opt/stic/.env

# Flask 配置
# 生产环境必须设置强密钥！使用以下命令生成：
# openssl rand -hex 32
SECRET_KEY=your-secret-key-here-change-in-production
FLASK_ENV=production
FLASK_APP=app.py

# 数据库配置
# SQLite 配置（默认）
DATABASE_URL=sqlite:////opt/stic/competition.db

# PostgreSQL 配置（如果使用 PostgreSQL，取消注释并修改）
# DATABASE_URL=postgresql://stic_user:your_password@localhost/stic_db

# 应用配置
UPLOAD_FOLDER=/opt/stic/uploads
CERTIFICATE_FOLDER=/opt/stic/certificates

# 可选：邮件配置（如果需要邮件功能）
# MAIL_SERVER=smtp.example.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your_email@example.com
# MAIL_PASSWORD=your_password

